# Example environment variables for chatbot_frontend
# Copy this file to .env and update values as needed for your environment.

# Base URL of the chatbot_backend (no trailing slash).
# For local dev, something like: http://localhost:8000
REACT_APP_BACKEND_URL=
